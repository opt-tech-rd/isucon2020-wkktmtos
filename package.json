{
	"devDependencies": {
		"npm-run-all": "^4.1.5",
		"prettier": "^2.1.2",
		"prettier-plugin-sh": "^0.6.0"
	},
	"scripts": {
		"format": "run-s format:*",
		"format:prettier": "prettier --write .",
		"format:goreturn": "goreturns -l -w ."
	}
}
